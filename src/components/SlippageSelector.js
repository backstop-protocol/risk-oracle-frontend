const SlippageSelector = props => {
  return (
    <div>
      <select id="slippage-selector">
        <option value="" defaultValue>5% slippage</option>
        <option>…</option>
      </select>
    </div>
  )
}

export default SlippageSelector